# LAQTA Admin Capabilities Guide
*Generated from codebase exploration — 2026-07-02*

---

## كيفية الدخول إلى وضع الإدارة / How to Access Admin Mode

- سجّل الدخول بحساب يحمل قيمة `role: "admin"` في قاعدة البيانات
- القيمة الحرفية في الكود: `AppConstants.roleAdmin = 'admin'`
- بمجرد تسجيل الدخول، يحصل المسؤول تلقائياً على **شريط تنقل سفلي مختلف تماماً** (5 تبويبات إدارية) بدلاً من الواجهة العادية للعملاء والمصورين
- يتم تخزين الدور (role) في `SharedPreferences` ويُحمَّل في كل مرة يفتح فيها التطبيق

---

## شاشات الإدارة / Admin Screens

### 1. لوحة الإدارة (Admin Dashboard)
**التبويب الأول في شريط التنقل**
- **الملف:** `lib/features/admin/presentation/screens/admin_dashboard_screen.dart`
- **API:** `GET /admin/stats`
- **ما تعرضه:** شبكة من 4 بطاقات إحصائية + 3 أزرار تنقل سريع

**الإحصائيات المعروضة:**
| الاسم العربي | المفتاح | الوصف |
|---|---|---|
| طلبات اليوم | `requestsToday` | عدد طلبات الحجز الواردة اليوم |
| إجمالي الحجوزات | `bookingsTotal` | إجمالي الحجوزات في المنصة |
| الإلغاءات | `cancellations` | عدد الحجوزات الملغاة |
| نزاعات مفتوحة | `openDisputes` | عدد النزاعات التي تنتظر الحل |

**أزرار التنقل السريع:**
- زر "النزاعات المفتوحة" → يفتح شاشة النزاعات
- زر "البلاغات" → يفتح شاشة البلاغات
- زر "المستخدمون" → يفتح شاشة إدارة المستخدمين

---

### 2. النزاعات المفتوحة (Open Disputes)
**التبويب الثاني في شريط التنقل**
- **الملف:** `lib/features/admin/presentation/screens/admin_disputes_screen.dart`
- **API:** `GET /api/v1/disputes` (عبر `DisputesDependencies.getOpenDisputes()`)
- **ما تعرضه:** قائمة بجميع النزاعات ذات الحالة `open` مرتبة تنازلياً

**ما يظهر لكل نزاع:**
- سبب النزاع (`reason`)
- معرّف الحجز المرتبط به (`bookingId`)
- تاريخ فتح النزاع

**الإجراءات المتاحة:**
- [ ] الضغط على أي نزاع يفتح **شاشة تفاصيل النزاع** (انظر القسم التالي)
- [ ] سحب للأسفل لتحديث القائمة (Pull to refresh)

---

### 3. تفاصيل النزاع (Dispute Details)
**شاشة تُفتح من داخل قائمة النزاعات**
- **الملف:** `lib/features/admin/presentation/screens/admin_dispute_details_screen.dart`
- **API للتحديث:** `PATCH /api/v1/disputes/{id}` + `PATCH /api/v1/bookings/{id}`
- **ما تعرضه:**
  - سبب النزاع وتفاصيله الكاملة
  - من فتح النزاع (`openedBy`)
  - بيانات الحجز المرتبط (النوع، التاريخ، الحالة) إذا كان متاحاً
  - حقل نصي لكتابة ملاحظة قرار الحل

**قرارات الحل المتاحة (3 خيارات):**

| الزر | الكود | ماذا يفعل |
|---|---|---|
| **حل: تحرير للمصور** | `release_to_photographer` | يُغلق النزاع ويضع الحجز كـ `completed` (يُسدَّد للمصور) |
| **حل: استرجاع للعميل** | `refund_full` | يُغلق النزاع ويضع الحجز كـ `canceled` (يُسترجع للعميل) |
| **حل: استرجاع جزئي** | `refund_partial` | يُغلق النزاع ويضع الحجز كـ `completed` مع ملاحظة استرجاع جزئي |

> **ملاحظة:** تغيير حالة الحجز يُسجَّل في `timeline` الحجز تلقائياً. لكن **تحريك الأموال فعلياً (Stripe/دفع)** يتطلب إجراءً من الباكيند — التطبيق يُغيّر الحالة فقط.

---

### 4. البلاغات (Reports)
**التبويب الثالث في شريط التنقل**
- **الملف:** `lib/features/admin/presentation/screens/admin_reports_screen.dart`
- **API:** `GET /admin/reports?sort=createdAt:desc`
- **ما تعرضه:** قائمة بجميع البلاغات مرتبة من الأحدث إلى الأقدم

**ما يظهر لكل بلاغ:**
- سبب البلاغ (`reason`)
- نوع البلاغ (`reportType`)
- اسم المُبلَّغ عنه (`reportedUserName`)
- حالة البلاغ (`status`)
- تاريخ البلاغ

**الإجراءات المتاحة (قائمة منبثقة):**
- [ ] **تمييز كمحلول** → `PATCH /admin/reports/{id}/status` بـ `{status: "resolved"}`
- [ ] **تجاهل** → `PATCH /admin/reports/{id}/status` بـ `{status: "dismissed"}`
- [ ] **حذف البلاغ** → `DELETE /admin/reports/{id}`

> **ملاحظة:** لا يوجد إجراء مباشر لمعاقبة المُبلَّغ عنه من هذه الشاشة — يجب الذهاب إلى شاشة المستخدمين لذلك.

---

### 5. المستخدمون (Users)
**التبويب الرابع في شريط التنقل**
- **الملف:** `lib/features/admin/presentation/screens/admin_users_screen.dart`
- **API:** `GET /admin/users?sort=createdAt:desc`
- **ما تعرضه:** قائمة بجميع المستخدمين مرتبة من الأحدث

**ما يظهر لكل مستخدم:**
- الاسم
- الدور (`role`) والمحافظة (`governorate`)
- أيقونة حمراء (`block`) إذا كان الحساب محظوراً

**الإجراءات المتاحة (قائمة منبثقة):**
- [ ] **حظر الحساب** → `PATCH /admin/users/{id}` (يضيف `'__admin_blocked__'` إلى `blockedUsers`)
- [ ] **إلغاء الحظر** → `PATCH /admin/users/{id}` (يُزيل `'__admin_blocked__'` من `blockedUsers`)
- [ ] **إرسال تحذير** → `POST /api/v1/notifications` (يُرسل إشعار نظام بعنوان "Account warning")

**آلية الحظر:**
- الحظر يضيف القيمة `'__admin_blocked__'` إلى مصفوفة `blockedUsers` الخاصة بالمستخدم
- في كل تشغيل للتطبيق، يتحقق الـ router من وجود هذه القيمة
- إذا كان المستخدم محظوراً يُعرض له **شاشة الحساب المحظور** التي تحتوي على زر تسجيل الخروج فقط

---

### 6. شاشة الحساب المحظور (Account Blocked Screen)
**لا تظهر للأدمن — تظهر للمستخدم المحظور**
- **الملف:** `lib/features/admin/presentation/screens/account_blocked_screen.dart`
- تُعرض تلقائياً لأي مستخدم يحاول فتح التطبيق وحسابه محظور
- تحتوي على رسالة توضيحية وزر **تسجيل الخروج** فقط

---

### 7. الحساب (Profile)
**التبويب الخامس في شريط التنقل**
- نفس شاشة الملف الشخصي (`ProfileScreen`) المستخدمة من الأدوار الأخرى
- تعرض بيانات حساب الأدمن وتسمح بتسجيل الخروج

---

## ما يستطيع الأدمن فعله / What Admin CAN Do

- [x] رؤية الإحصائيات الرئيسية (طلبات اليوم، إجمالي الحجوزات، الإلغاءات، النزاعات)
- [x] عرض جميع النزاعات المفتوحة ومراجعة تفاصيلها
- [x] حل النزاعات بثلاثة خيارات (تحرير للمصور / استرجاع للعميل / استرجاع جزئي)
- [x] كتابة ملاحظة نصية مع كل قرار نزاع
- [x] عرض جميع البلاغات وتصفيتها يدوياً
- [x] تمييز البلاغات كـ "محلول" أو "مُتجاهَل"
- [x] حذف البلاغات
- [x] عرض قائمة بجميع المستخدمين المسجلين
- [x] حظر أي حساب مستخدم (يمنعه من استخدام التطبيق فوراً)
- [x] إلغاء حظر أي حساب
- [x] إرسال تحذير (إشعار) لأي مستخدم

---

## ما لا يستطيع الأدمن فعله من التطبيق / What Admin CANNOT Do

- [ ] **حذف حسابات المستخدمين** — يمكن الحظر فقط، لا الحذف الفعلي
- [ ] **البحث أو الفلترة** في المستخدمين أو البلاغات — القائمة كاملة فقط (بدون بحث)
- [ ] **التصفح برقمة (Pagination)** — جميع القوائم تُحمَّل دفعة واحدة
- [ ] **تحريك الأموال مباشرةً** — القرارات تُغيّر حالة الحجز فقط، الاسترجاع الفعلي يحتاج تدخل يدوي في Stripe
- [ ] **الاطلاع على تفاصيل الحجوزات** بشكل مستقل (يُرى فقط كجزء من تفاصيل النزاع)
- [ ] **الموافقة على توثيق المصورين أو رفضها** — لا توجد شاشة verification للأدمن في التطبيق
- [ ] **إدارة المحتوى** (أماكن، إعلانات، جلسات) — لا يوجد CRUD للمحتوى في لوحة الأدمن
- [ ] **رؤية تحليلات الإيرادات** — لا توجد شاشة analytics للأدمن
- [ ] **إنشاء حسابات أدمن جديدة** — يتطلب تعديل قاعدة البيانات مباشرة
- [ ] **إضافة ملاحظة على البلاغات** — حقل الملاحظة موجود فقط في شاشة النزاعات
- [ ] **معرفة تفاصيل المُبلَّغ عنه** (صورة، ملفه الشخصي) من شاشة البلاغات مباشرة

---

## الفجوات الموصى بمعالجتها / Recommended Next Steps

### عالية الأولوية
1. **Pagination / بحث وفلترة** في شاشتَي المستخدمين والبلاغات — حالياً تُحمَّل كل السجلات دفعة واحدة مما سيُبطئ التطبيق مع نمو قاعدة البيانات
2. **ربط قرار النزاع بالاسترجاع الفعلي في Stripe** — حالياً القرار يُغيّر الحالة فقط والاسترجاع يدوي

### متوسطة الأولوية
3. **شاشة توثيق المصورين** — الموافقة/الرفض على طلبات التوثيق من داخل التطبيق
4. **تفاصيل المستخدم** — صفحة تُظهر ملف المستخدم الكامل وسجل حجوزاته قبل اتخاذ قرار الحظر
5. **فلتر البلاغات حسب الحالة** (pending/resolved/dismissed) — حالياً تظهر كل البلاغات معاً

### منخفضة الأولوية
6. **لوحة تحليلات** (إيرادات، نمو المستخدمين، أكثر الأماكن طلباً)
7. **إمكانية الرد المباشر على المستخدم** من شاشة البلاغات بدلاً من الإشعار التلقائي فقط

---

## ملخص API endpoints الخاصة بالأدمن

| Method | Endpoint | الاستخدام |
|---|---|---|
| `GET` | `/admin/stats` | إحصائيات لوحة الإدارة |
| `GET` | `/admin/users?sort=createdAt:desc` | قائمة المستخدمين |
| `PATCH` | `/admin/users/{id}` | حظر/إلغاء حظر مستخدم |
| `GET` | `/admin/reports?sort=createdAt:desc` | قائمة البلاغات |
| `PATCH` | `/admin/reports/{id}/status` | تحديث حالة بلاغ |
| `DELETE` | `/admin/reports/{id}` | حذف بلاغ |
| `GET` | `/api/v1/disputes` | النزاعات المفتوحة |
| `PATCH` | `/api/v1/disputes/{id}` | حل نزاع |
| `PATCH` | `/api/v1/bookings/{id}` | تحديث حالة الحجز عند حل النزاع |
| `POST` | `/api/v1/notifications` | إرسال إشعار تحذير للمستخدم |
